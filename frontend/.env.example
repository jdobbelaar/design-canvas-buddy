# Base URL of the FastAPI backend (see backend/). If unset, dev builds use
# http://localhost:8000 and production builds use the page's own origin
# (the Docker image serves the frontend from the backend). Copy this file to
# .env.local and adjust if your backend runs elsewhere.
VITE_BACKEND_URL=http://localhost:8000
