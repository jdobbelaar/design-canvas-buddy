# Base URL of the FastAPI backend (see backend/). Defaults to
# http://localhost:8000 if unset. Copy this file to .env.local and adjust
# if your backend runs elsewhere.
VITE_BACKEND_URL=http://localhost:8000
